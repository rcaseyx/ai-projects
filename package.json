{
  "scripts": {
    "find": "npm run find --prefix apply-agent",
    "apply": "npm run apply --prefix apply-agent",
    "track": "npm run track --prefix apply-agent",
    "interview": "npm run interview --prefix apply-agent",
    "digest": "npm run digest --prefix apply-agent"
  }
}
